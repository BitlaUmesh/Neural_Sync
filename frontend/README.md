# Neural-Sync

Neural-Sync is an AI-powered Git conflict resolution extension using a local or cloud LLM backend.

## Features
- Deep Analysis of Git Conflicts
- Surgical Merging logic using LLMs via a FastAPI backend

## Setup
- Start the server from the backend repository
- Click the UI button when a conflict is encountered.
